# Agent Shield wrapper configuration

# Ollama connection
AGENT_SHIELD_OLLAMA_HOST=http://localhost:11434
AGENT_SHIELD_OLLAMA_MODEL=qwen3.5:0.8b

# Backend approval API
AGENT_SHIELD_BACKEND_URL=http://localhost:3000

# Codex subprocess invocation
# Option A: use a `codex` binary available on your PATH
AGENT_SHIELD_CODEX_COMMAND=codex
AGENT_SHIELD_CODEX_ARGS=--dangerously-skip-possible-errors

# Option B: run via npx (uncomment to use)
# AGENT_SHIELD_CODEX_COMMAND=npx -y @anthropic-ai/codex@latest
# AGENT_SHIELD_CODEX_ARGS=--dangerously-skip-possible-errors

